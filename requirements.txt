# WaveMaker Docs Agent - Dependencies
# Python 3.13 compatible (January 2026)

# Core API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0  # Server-Sent Events for streaming

# LLM - Anthropic Claude
anthropic>=0.40.0

# LLM - OpenAI (also used for Ollama's OpenAI-compatible API)
openai>=1.59.0

# Vector Database - Qdrant Cloud
qdrant-client>=1.12.0

# Embeddings (sentence-transformers with ONNX for speed)
sentence-transformers>=3.3.0
# onnxruntime>=1.19.0
transformers>=4.46.0
tokenizers>=0.20.0
torch>=2.0.0  # Explicit PyTorch (avoids MLX issues on Apple Silicon)

# Redis Cache
redis>=5.2.0
hiredis>=3.0.0  # Faster Redis parser

# Postgres (async)
asyncpg>=0.29.0

# HTTP Client (async)
httpx>=0.27.0

# Model Context Protocol (MCP) - Official SDK for Academy integration
mcp>=1.2.0  # Python 3.13 compatible

# Markdown Parsing
python-frontmatter>=1.1.0
mistune>=3.0.2
pyyaml>=6.0.2

# Configuration
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Utilities
numpy>=2.0.0
tenacity>=9.0.0  # Retry logic

# Development & Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# Git operations (for cloning docs repo)
gitpython>=3.1.0
